name: Build and Test

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Test imports and syntax
        run: |
          python -c "import sqlite3, os; print('All imports successful')"
          python -m py_compile app.py
          echo "Code compilation successful"







          
############################################
#Vad denna pipeline gör är 
#När pushar kod till GitHub:
############################################
#GitHub startar en Linux VM
#Python 3.9 installeras
#kod testas genom att:
############################################
#import sqlite3
#import os
############################################
#python -m py_compile app.py
############################################